

0. git config --global user.name "Your Name"
   git config --global user.email you@example.com

1. 폴더 생성
2. git init ==> .git 폴더 생성 확인
해당 폴더 마우스 오른쪽 버튼 -> git bash here 클릭
>ssh-keygen
>cat ~/.ssh/id_rsa.pub
ssh-rsa 부터 시작한 텍스트들을 모두 드래그로 복사
github에 로그인을 하시고, 우측상단의 메뉴 버튼을 클릭후 setting > SSH and GPG keys
SSH Keys 신규 등록

>git clone git@github.com:goskang/study.git

sss

3. git status ==> git 상태관리
4. git add 파일명 / git add * ==> git 파일 트레킹
5. git commit -m "코멘트" ==> 커밋
5. git branch -M main ==> 메인 브랜치 이동
6. git remote add origin git@github.com:goskang/study.git
7. git push -u origin main
8. git log 
9. git clone https://github.com/goskang/study.git